# Servidor
PORT=4000
NODE_ENV=development

# Base de datos
DB_HOST=192.168.69.163
DB_PORT=3306
DB_USER=tu_usuario
DB_PASSWORD=tu_contraseña
DB_NAME=control_impresoras

# JWT (autenticación de usuarios)
JWT_SECRET=cambia_esto_por_un_secreto_largo_y_aleatorio
JWT_EXPIRES_IN=8h

# Dolibarr
DOLIBARR_URL=https://tu-dolibarr.com/api/index.php
DOLIBARR_API_KEY=tu_api_key_dolibarr

# API key legacy (para scripts/cron que no usan JWT)
API_KEY=tu_api_key_interna

# CORS (en producción, la URL del frontend)
CORS_ORIGIN=*
